# SafeNap — Memória de Longo Prazo

Log append-only de sessões de IA/humanas. Entradas mais antigas no topo, novas no fim.
Nunca apagar ou editar entradas antigas. Estado técnico atual: `.ai/state.md`.

Formato:

```
## [YYYY-MM-DD] <IA/humano> | <resumo de uma linha>
- Feito: ...
- Decisões: ...
- Lições: ...
- Próximos passos: ...
```

---

## [2026-09-12] bootstrap | Memória do projeto inicializada
- Feito: criados este log, `.ai/state.md`, seção de memória no AGENTS.md e CLAUDE.md.
- Decisões: memória vive no repo (arquivos markdown), não em serviço externo — qualquer IA lê/registra; versionada junto com o código.
- Lições: nenhuma ainda.
- Próximos passos: preencher `.ai/state.md` com a stack real na primeira sessão de trabalho.

## [2026-09-19] Claude (Sonnet 5) | Auditoria completa da camada de ML/CV + correções + pipeline reprodutível
- Feito: auditoria arquivo a arquivo; `docs/ML_PIPELINE.md` (auditoria, taxonomia IMPLEMENTADO/VALIDADO/EXPERIMENTAL/NÃO VALIDADO, como reproduzir). `shared/feature_schema.json` = fonte única das 18 features (testes de paridade por posição Py↔TS). Python (`ml/features/`) reescrito para espelhar `frameAnalyzer.ts`/`featureExtractor.ts`/DetectionEngine (fixture `shared/feature_golden.json`, igualdade a 1e-9 com o código TS real). `extract_features.py` (FaceLandmarker tasks, manifesto, estado por vídeo), `train_model.py` (holdout por sujeito, GroupKFold, 4 candidatos, ONNX+model card, bytes reprodutíveis), `evaluate.py` (estava quebrado), `ml/evaluation/metrics.py`. Frontend: `predictCART` (feature índice 0 tratada como folha), sentinela -1/NaN unificados (`featureVectorToArray`), `drowsinessModel` (descarte de resultado antigo por geração, timeout, status de erro, validação de I/O, sem fallback de modelo por CDN de terceiros, WASM alinhado à versão do pacote), política ML×regras. Testes: TS 132→196, Python 7→39.
- Decisões (usuário): (1) rebaixar o ML — pode gerar WARNING, NÃO origina ALARM sozinho (ML≥0,95 só escala para ML_ALARM com regra de aviso fisiológica ativa; FACE_LOST não conta). (2) Só o CEW como dado real por enquanto: NTHU/UTA-RLDD NÃO estão disponíveis → nenhum treino real, nenhuma métrica de sonolência.
- Lições: o ONNX embarcado é RF treinado em dados SINTÉTICOS (~3 nós/árvore); a doc antiga (train.py, int8, NTHU/UTA, ml/README.md, export_onnx.py) descrevia coisas inexistentes. Escala sintética do noseDrop ≠ real → pessoa acordada pontua P(drowsy)≈0,39. ORT-web 1.27 devolve `probabilities=[-p,p]` e `label=1` neste modelo (ORT Python: `[1-p,p]`) — só o índice 1 é confiável; o frontend só lê o índice 1. skl2onnx grava UUID aleatório em `graph.name` (normalizado). O `.venv` não tinha pandas (não foi criado do requirements). onnxruntime-web instalado 1.27 vs WASM do CDN fixo em 1.22 (corrigido). O cooldown de MICROSLEEP não bloqueia re-alarme (falha-segura, só caracterizado em teste). Modos `ml` e `hybrid` são equivalentes.
- Removidos (justificado): `ml/scripts/generate_dummy_model.py` (escrevia no caminho de produção com dados aleatórios) e `ml/frontend/public/models/drowsiness.onnx` (órfão, 60 árvores de ruído). `onnxoptimizer` saiu do requirements (sem uso).
- Próximos passos: obter NTHU/UTA-RLDD, decidir o mapeamento de rótulos (UTA 0/5/10 → binário), montar o manifesto e rodar o pipeline real; decidir se o modelo do usuário (destilado do ONNX sintético, sem holdout, prioridade sobre o ONNX) deve continuar; auto-hospedar o WASM do ORT; medir MediaPipe/ORT em navegador real; rodar `code-reviewer`/`security-reviewer` (não rodados).

## [2026-09-19] Claude (Sonnet 5) | Revisões, correções de segurança e WASM do ORT auto-hospedado
- Feito: code-reviewer + security-reviewer rodados; corrigidos validação de modelo remoto/persistido (`validateUserRF`), amostras não finitas, chaves localStorage v2, timeout→status error, gate de ML_ALARM na fusão, manifesto restrito a --raw, `evaluate.py --model-card`. WASM do ORT agora vem do mesmo origin via `?url` do Vite + entry `onnxruntime-web/wasm`; verificado em Chromium (status ready, score = ORT Python) e no build de produção. Branch `feat/ml-audit-pipeline` publicada.
- Lições: o bundle padrão do onnxruntime-web pede a variante jsep (WebGPU); o dev server do Vite recusa JS de /public como módulo (usar `?url` de node_modules). Só o navegador real revelou isso (Node e testes unitários passavam).
- Próximos passos: hash do ONNX em runtime (opcional), CSP, obter NTHU/UTA e rodar o pipeline real, e2e do app completo com câmera mockada conferindo o MlStatusCard.

## [2026-09-20] Claude (Sonnet 5) | Verificação do checklist de correção cirúrgica (detecção/ML) + modelo do usuário fora do caminho de segurança
- Feito: cada ponto do checklist foi verificado no código atual. JÁ CORRIGIDOS na sessão de 2026-09-19 (não refeitos): fórmula de noseDropRatio idêntica TS↔Python (`frameAnalyzer.ts:153` = `metrics.py:99`, golden a 1e-9); Python usa FaceLandmarker tasks (sem `mp.solutions`); features offline sem placeholder (blinkRate/PERCLOS via BlinkTracker espelhando o DetectionEngine); split por sujeito (GroupShuffleSplit + GroupKFold, teste de interseção vazia); ONNX↔Python coberto por `onnx_golden.json`/`test_model_contract`; ML antigo descartado (ML_STALE_MS, geração/epoch, timeout). CONFIRMADO e corrigido agora: auto-treino em runtime — `mlDataCollector` treinava o RF do usuário a cada 30 s e `drowsinessModel` o priorizava sobre o ONNX. Agora atrás de `VITE_ENABLE_USER_MODEL` (padrão off; `isUserModelEnabled()` em thresholds.ts).
- Decisões: mudança mínima (flag), infraestrutura experimental preservada; treino manual segue disponível mas não afeta a segurança quando o flag está off.
- Lições: o prompt de correção assumia problemas que a auditoria anterior já tinha resolvido — verificar antes de editar evitou retrabalho. Modelo ONNX continua EXPERIMENTAL/SINTÉTICO sem métricas; não há dataset real para retreino (NTHU/UTA-RLDD ausentes), então nenhuma métrica nova foi calculada.
- Testes: TS 216 (20 arquivos), Python 41, tsc e build OK. Lint: só warnings pré-existentes de React em componentes.
- Próximos passos: obter dataset real e rodar o pipeline; MlTrainingCard ainda aparece na UI com o flag off (decidir se esconde); auditar `signalFusion`/VisionQuality com dados reais (sem lacuna comprovada nesta sessão).

## [2026-09-20] Claude (Sonnet 5) | Fechamento do pipeline de ML: UI experimental, adaptadores de dataset, relatório reproduzível
- Feito: `MlTrainingCard` agora retorna null sem `VITE_ENABLE_USER_MODEL=true` (e se rotula "Experimental"; texto "Modelo ativo" trocado). `ml/dataset_adapters/` (UTA-RLDD por arquivo/pasta, NTHU-DDD por tabela de pares + anotação por quadro) + `scripts/build_manifest.py`; mapeamento de rótulos explícito (UTA 5 = EXCLUÍDO). `extract_features.py`: `video_id` = caminho relativo (antes `video.stem` colidia entre sujeitos: '0','5','10'); várias linhas do mesmo vídeo = 1 passada com rótulo por instante. `train_model.py`: `assert_disjoint` (treino/teste e cada fold), contagens por partição (sujeitos/vídeos/clipes/linhas/pos/neg), métrica por CLIPE (só com >=2 clipes/classe), `model_version`, `experiment.json` + `confusion_matrix.csv`, `--dataset-name`. `docs/ML_PIPELINE.md` §13 (4 tipos de validação, rótulos, unidade de avaliação). `download_datasets.py` deixou de afirmar layouts não verificados.
- Decisões: pacote chama `dataset_adapters` (não `datasets`: colide com HuggingFace `datasets` se instalado). Sem métrica por sujeito (poucos sujeitos por classe). NENHUM treino real, nenhum dado sintético apresentado como validação.
- Lições: layouts do UTA/NTHU são SUPOSIÇÃO (sem o dataset aqui) — adaptadores falham com `DatasetLayoutError` em vez de adivinhar; ajustar após inspecionar o download. Smoke test da CLI achou `UnicodeEncodeError` (seta "→" em print no console cp1252 do Windows) — nunca usar caracteres fora de cp1252 em print de scripts. Heredocs longos no Bash falham por aspas; usar Write + script.
- Testes: Vitest 219 (21 arq.), pytest 55, tsc e build OK; lint só com warnings antigos.
- Próximos passos: obter UTA-RLDD/NTHU-DDD, conferir layout real contra os adaptadores, rodar build_manifest -> extract_features -> train_model e registrar a validação de drowsiness; depois validação em condições reais. code-reviewer/security-reviewer ainda não rodados sobre esta mudança.

## [2026-09-20] Claude (Sonnet 5) | Auditoria final do pipeline de dataset (sem dataset real, sem DetectionEngine)
- Feito: `ml/dataset_adapters/` reescrito (LabelMap com status; TODO mapa de inclusão nasce UNVERIFIED e só entra com `--confirm-labels <id>`; problemas coletados, não lançados; sujeito/vídeo com namespace do dataset), `validation.py` (manifesto: existência, tamanho, duplicatas, sobreposição, mistura de sujeitos, `subject_key` p/ identidade duvidosa, arquivos idênticos, classes, `--probe`; CSV de features: timestamps, vídeo x sujeito, vetores duplicados, classes), `build_manifest.py` (+`--dry-run`, `dataset_summary.json`), `validate_manifest.py`, `extract_features.py` (descartes contados por motivo, `frame_index`/`dataset`/`relative_path`/`clip_id`, `.partial` só promovido se válido, `--dry-run`, `--skip-unreadable`, `_extraction_report.json`), `train_model.py` (valida o CSV na entrada; barreira de vídeo/clipe no holdout e nos folds). `docs/DATASET_PIPELINE.md` (auditoria por adaptador, tabelas de rótulo, procedimento de 17 passos).
- Decisões: mapas de rótulo exigem confirmação humana explícita (id do mapa muda => reconfirmar). Etapas 1-4 (manifest, extração, treino) nunca encadeiam. `--dry-run` SIMULA a confirmação só para checar layout e diz isso.
- Lições: layouts UTA/NTHU seguem SUPOSIÇÃO (sem os arquivos); testes com árvores fabricadas validam o adaptador, não a correspondência com o dataset oficial. Mutação (desligar gate de rótulo / promoção do CSV / reset de estado) derrubou os testes certos. Fixture sintético "fechado" (openness 0.1) tem EAR 0.255 > 0.21: testes de piscar usam `ear_threshold=0.30`. Rodar `npx` com cwd errado tenta baixar pacote `tsc` falso: usar caminho absoluto. Nunca usar setas/símbolos fora de cp1252 em `print` de scripts.
- Testes: pytest 96, Vitest 219, tsc/build OK. Frontend/DetectionEngine intocados.
- Próximos passos: obter UTA-RLDD/NTHU-DDD e executar o procedimento de `docs/DATASET_PIPELINE.md` §9 (começa por ler a doc oficial e rodar `--dry-run`). code-reviewer/security-reviewer NÃO rodados sobre esta mudança.

## [2026-09-23] Claude (Sonnet 5) | SLOW_BLINKS falso-positivo (motor vibrando com atenção normal) + diagnóstico do "alarme preso"
- Feito: relato real de uso — motor/buzzer ligando com os olhos abertos. Reproduzido ao vivo contra o `DetectionEngine` real (piscadas com rampa gradual de EAR, não degrau): piscadas rápidas (50-200ms de fechamento) classificam certo como normais, mas qualquer fechamento entre `maxBlinkMs` (400ms) e `microsleepAlarmMs` (1800ms) contava igualmente como "piscada lenta", e só 3/min já bastava pra `SLOW_BLINKS` (que o backend traduz em `VIBRATION_ON`). Corrigido: novos campos `slowBlinkMinMs`/`slowBlinkRateThreshold` em `DetectionConfig` — zona-morta entre `maxBlinkMs` e `slowBlinkMinMs` (nem normal nem lenta, absorve a inflação de medição da histerese de reabertura + filtro de mediana) e limiar de taxa maior (lenient 700ms/5min, standard 550ms/4min, strict 450ms/3min). 3 testes de regressão em `detectionEngine.test.ts` (mutation-testados: revertidos os valores, confirmado que o teste de "3 piscadas" falha sem o fix).
- Investigado à parte (SEM mudança de código, aguardando confirmação do usuário): relato anterior de "alarme não sai mesmo com olhos abertos" — reproduzido ao vivo e a liberação da câmera funciona (4 ciclos fechar→ALARM→abrir liberaram certo). Causa mais provável, pelo código: `SafetyManager` funde visão+garra por OR de severidade (`max(_vision_state, _grip_state)`), e `GripMonitor` é deliberadamente sem corte automático (`_evaluate_state`: "enquanto a queda persistir, o estado permanece ALARM indefinidamente") — se a mão não está firme no sensor de pressão durante um teste focado na câmera, o alarme de garra sozinho mantém tudo ligado. Proposto (não implementado): mostrar no painel qual fonte (câmera vs. garra) está causando o alarme.
- Decisões: threshold de SLOW_BLINKS é por preset (não um valor único), seguindo o padrão já usado pelos outros campos de `DetectionConfig`.
- Lições: teste sintético de duração exata (`tick(frame(...), ms fixo)`) não pega esse tipo de falso-positivo — só apareceu simulando uma rampa de EAR gradual com cadência real (~33ms/frame) rodando contra o motor de verdade num browser. Mutation testing (reverter o fix e confirmar que o teste novo falha) continua sendo o jeito de saber se um teste de regressão é real.
- Testes: Vitest 223 (+3), tsc e build OK.
- Próximos passos: confirmar com o usuário se a causa do "alarme preso" é mesmo o sensor de garra sem mão firme; se for, decidir se implementa o indicador de fonte (câmera vs. garra) no painel. `docs/DETECTION.md` já atualizada com os novos números de SLOW_BLINKS.

## [2026-09-23] Claude (Sonnet 5) | Auditoria ampla de falsos-positivos de detecção: HEAD_DROP corrigido, PERCLOS/YAWN(fala rápida) confirmados OK
- Feito: usuário pediu auditoria geral ("muitos sinais não são o caso real na prática"). Testado ao vivo contra o `DetectionEngine` real (câmera simulada, transições graduais, cadência real ~33ms/frame): (1) PERCLOS com ~9 piscadas normais reais (150ms de fechamento) em 6,5s — ficou em 0, todas corretamente classificadas como piscada normal, `perclosIgnoreMs` filtra bem; (2) fala rápida (boca oscilando acima/abaixo do limiar de bocejo, nunca sustentada) — não disparou YAWN, o requisito de `yawnMinMs` sustentado já protege contra isso; (3) **cabeça baixa por 2,5s olhando pro painel/celular, olhos bem abertos (ear=0.40) o tempo todo — disparava HEAD_DROP mesmo assim**, porque a detecção olhava só a posição do nariz, nunca o estado do olho.
- Corrigido: `processHeadDrop` agora recebe `eyesLookAlert` (ear > threshold×hysteresisFactor, mesmo limiar já usado pra "olhos confiantemente abertos" em outro lugar do arquivo) e só confirma HEAD_DROP se os olhos também não parecerem alertas no momento da confirmação — queda de cabeça por sonolência real sempre vem com a pálpebra caindo junto (mesmo relaxamento muscular); um olhar alerta pra baixo não. 3 cenários em `detectionEngine.scenarios.test.ts`: confirmado COM pálpebra caindo (ear=0.22, zona ambígua entre threshold e threshold×hysteresis — não fecha por completo, só não está "alerta"), confirmado que dura pouco não dispara, e o cenário do relato real (ear=0.35, olhos bem abertos) agora fica NORMAL. Mutation-testado (revertido o gate, confirmado que o cenário novo falha sem ele).
- Decisões: não mexi em YAWN além do já testado — fala rápida (sílabas oscilando) está protegida, mas uma fala SUSTENTADA com boca bem aberta por >400ms seguidos (ex: "aaah", risada) ainda dispararia igual a um bocejo real — limitação conhecida do sinal único de aspect ratio da boca, não resolvida (nenhum relato específico disso ainda, não investi em corrigir algo não confirmado).
- Lições: escolher o EAR certo pra isolar um cenário sintético importa — minha primeira tentativa de verificação ao vivo usou ear=0.15 pro "controle com pálpebra caindo", que fica abaixo do threshold e dispara EYES_CLOSED_DURATION (ALARM) antes mesmo do HEAD_DROP confirmar, confundindo o sinal. ear=0.22 (entre threshold e threshold×hysteresisFactor) isola melhor: "não alerta" sem entrar no caminho de "olhos fechados". Um "FACE_LOST" espúrio apareceu rodando dois casos de teste na mesma página sem resetar tudo direito — artefato do script, não bug real (confirmado limpo numa página nova).
- Testes: Vitest 224 (+1), tsc e build OK. `docs/DETECTION.md` atualizada.
- Próximos passos: nenhuma auditoria feita ainda em EAR_TREND (tendência de queda gradual) nem PERCLOS_CRITICAL/MICROSLEEP em cenários reais de luz variável/ângulo de câmera — ficam como possível próxima rodada se o usuário reportar mais falsos-positivos.

## [2026-09-23] Claude (Sonnet 5) | Auditoria completa pré-demonstração: calibração não resetava entre pessoas (frontend + garra no backend) + gate de avaliação inconsistente
- Contexto: usuário pediu auditoria completa pensando num dia com muita gente testando o mesmo dispositivo/laptop. Terminei a rodada de sinais (EAR_TREND com deriva benigna de até 10% em 45s: ficou NORMAL, sem problema) e daí investiguei o fluxo de calibração/sessão, que é o que mais importa pra "gente diferente testando em sequência".
- **Achado 1 (frontend, o mais sério pra esse cenário): `calibrationManager.skipWithDefault()` ("Pular" no wizard) não resetava nada.** Dois problemas: (a) não parava uma calibração ainda em andamento — o wizard já dispara `startCalibration()` em segundo plano assim que abre, antes de qualquer clique; se a pessoa clicasse "Pular" rápido, `isCalibrating` continuava `true` por vários segundos (até a coleta de fundo completar ou estourar timeout sozinha), e a avaliação ficava muda nesse meio-tempo; (b) não resetava `threshold`/`baselineEar`/`baselineNoseDrop` — esses campos persistem em localStorage entre sessões no MESMO navegador, então "Pular" continuava silenciosamente usando a calibração (olhos e postura) da ÚLTIMA pessoa que usou o dispositivo, não um padrão neutro. Corrigido: `skipWithDefault()` agora para a coleta, limpa localStorage e volta tudo pro padrão neutro (`DEFAULT_THRESHOLD`, baselines null).
- **Achado 2 (mesma família de bug, backend): o sensor de pressão FSR nunca resetava a baseline ao trocar de pessoa.** `grip_monitor.reset()` só era chamado em queda/reconexão serial — nunca ao trocar de detector/sessão. Como o sensor é hardware físico persistente (independe do ciclo de vida da sessão do navegador), a força de aperto de quem calibrou a baseline primeiro continuava valendo pra todo mundo depois. Corrigido: `grip_monitor.reset()` agora também roda em todo `DETECTOR_CLAIM` bem-sucedido (`websocket/manager.py`).
- **Achado 3 (consistência do gate de avaliação): `processFrame()` só checava `!isCalibrating`, não `canEvaluate()` (que `processNoFace()` já checava).** Quando uma calibração FALHA (timeout/gap — `abort()` já zera `isCalibrating`), a avaliação ficava liberada mesmo sem threshold válido, na janela entre o erro aparecer na tela e o usuário escolher "Tentar de novo" ou "Pular". Corrigido: mesmo gate duplo nos dois métodos agora.
- Verificado e sem problema: PERCLOS com piscadas normais reais (0%), bocejo com fala rápida oscilando (não sustenta 400ms, não dispara), EAR_TREND com deriva benigna de até 10%/45s.
- Decisões: tratei "Pular" como devendo sempre cair num estado neutro, nunca herdar calibração de outra pessoa — coerente com o objetivo explícito do usuário (dispositivo compartilhado, muita gente testando no mesmo dia).
- Lições: bugs de "estado que deveria resetar por sessão mas persiste" tendem a vir em pares frontend/backend quando há hardware físico envolvido — encontrar um (calibração do olho no frontend) foi o que motivou procurar o equivalente no sensor de pressão. Todos os 3 achados foram mutation-testados (revertido cada fix, confirmado que o teste novo correspondente falha, depois restaurado).
- Testes: Vitest 227 (+3: 2 em calibrationManager.test.ts, 1 em detectionEngine.test.ts), pytest 137 (+1 em test_websocket_manager.py), tsc e build (frontend) OK. `docs/HARDWARE.md` atualizada.
- Próximos passos: nenhum backend real de demonstração testado ainda ponta-a-ponta (Arduino físico + várias pessoas em sequência) — os 3 fixes são verificados por teste unitário/integração, não por um teste de bancada com hardware real.

## [2026-09-23] Claude (Sonnet 5) | Refatoração arquitetural completa da camada de detecção (auditoria + fases 2-5)
- Contexto: pedido explícito e detalhado do usuário (21 seções) para auditar a arquitetura de detecção antes de mexer, opinar tecnicamente com evidência de código, avaliar uma proposta de estrutura modular, e só então refatorar em fases mantendo os testes como rede de segurança.
- Auditoria (sem alterar código): identificados 4 problemas reais — (1) DOIS mecanismos de fusão coexistindo (`signalFusion.ts` scoring contínuo vs. `mlReasons.ts` cadeia if/else, com precedência implícita); (2) PERCLOS usava denominador de relógio fixo (60000ms) mesmo durante perda de rosto, diluindo a métrica; (3) estado do olho (OPEN/CLOSING/CLOSED/OPENING) só existia como 4 campos privados soltos, nunca nomeado, só testável via `DetectionEngine` inteiro; (4) `detectionEngine.ts` (898 linhas) conhecia diretamente `sessionStats`/`wsClient`, violando separação de camadas. Confirmado que calibração JÁ é relativa ao usuário (não fixa) e ML JÁ é corroborativo (nunca origina ALARM sozinho) — não são problemas, ao contrário do que a auditoria antecipava como risco.
- 3 decisões de escopo confirmadas explicitamente pelo usuário antes de implementar: unificar signalFusion+mlReasons já na Fase 2; corrigir o denominador do PERCLOS; manter saída de 3 estados (NORMAL/WARNING/ALARM, o que o hardware consome) com um score contínuo interno em vez de expor um 4º nível (EARLY_FATIGUE).
- Implementado (fases 2-5, com testes a cada módulo antes de integrar):
  - `temporal/clock.ts` (Clock injetável + FakeClock) — substitui `Date.now()` espalhado.
  - `eye/eyeStateDetector.ts` (OPEN/CLOSING/CLOSED/OPENING/UNKNOWN, mesma histerese de antes, só nomeada), `eye/blinkDetector.ts`, `eye/perclosTracker.ts` (COM a correção do denominador), `eye/microsleepDetector.ts`.
  - `head/headDropDetector.ts`, `mouth/yawnDetector.ts`, `temporal/earTrendTracker.ts` — extrações preservando comportamento exato (incluindo o fix de corroboração ocular do HEAD_DROP de sessão anterior).
  - `vision/visionQuality.ts` (tipos GOOD/DEGRADED/LOST, `Signal<T>`) — criado mas ainda NÃO integrado ao pipeline principal (`frameAnalyzer.ts` continua retornando `null`/não-null; a gradação DEGRADED fica como trabalho futuro, NEEDS VALIDATION).
  - `fusion/signalFusion.ts` — módulo ÚNICO substituindo `signalFusion.ts` antigo + `ml/mlReasons.ts` (ambos deletados, órfãos). 3 camadas explícitas (fortes/ML-corroborativo/suporte-contínuo), mesma prioridade e limiares de antes, `FusionResult` agora com `primaryReason` + `contributingSignals` pra explicabilidade.
  - `detectionEngine.ts` reescrito como orquestrador fino: delega a cada detector, só mantém a histerese de entrada/saída de estado (que depende do estado anterior) e o despacho de efeitos colaterais.
- Testes: 56 testes novos nos módulos extraídos (todos isolados, com `FakeClock`, sem mockar `Date` global). A suíte de 227 testes de ponta-a-ponta que já existia (`detectionEngine.test.ts`, `.ml.test.ts`, `.scenarios.test.ts`) passou **sem nenhuma alteração de asserção** após a reescrita completa — confirma que o comportamento público não mudou, exceto o PERCLOS. 1 regressão real pega pelos testes existentes durante a reescrita (esqueci de portar `featureExtractor.reset()`/`drowsinessModel.reset()` de `processNoFace()`) — corrigida na hora. Total final: 254 testes unitários/integração + 15 e2e Playwright, todos verdes. tsc e build de produção OK.
- Mudança de comportamento DELIBERADA e documentada (única desta refatoração): PERCLOS agora usa tempo válido observado como denominador em vez de janela de relógio fixa — ver `eye/perclosTracker.ts` e `docs/DETECTION.md`.
- Decisões: não criei um arquivo `decision/decisionEngine.ts` separado (a proposta original do usuário sugeria) — a histerese de entrada/saída de estado depende do `this.state` anterior do orquestrador e ficou pequena o suficiente dentro de `detectionEngine.ts` depois da extração dos detectores; não força separação artificial sem necessidade real (YAGNI).
- Lições: reescrever uma classe de 900 linhas mantendo os 227 testes de ponta-a-ponta como única fonte de verdade (sem tocar nas asserções deles) é um teste de regressão muito mais forte do que qualquer revisão manual — pegou exatamente 1 lacuna real (reset do ML em processNoFace) que uma leitura cuidadosa provavelmente teria deixado passar.
- Próximos passos: `vision/visionQuality.ts` criado mas não usado no pipeline real ainda (frameAnalyzer.ts não distingue DEGRADED de LOST); parâmetros sinalizados como NEEDS VALIDATION no código/docs (clamp do threshold de calibração, pesos da fusão contínua) continuam sem dado experimental — não inventados, só documentados como pendentes; nenhuma validação com dataset real ainda (mesma pendência de sempre, ver entradas de ML anteriores).

## [2026-09-23] Claude (Sonnet 5) | Alarme de garra ficava ligado para sempre com sessão fechada + teto de 30s adicionado
- Contexto: usuário perguntou se os sinais pro Arduino morrem ao fechar/desligar uma sessão, e pediu uma forma de parar de alertar depois de muito tempo sem pressão no FSR.
- Achado (respondendo a pergunta): NÃO de forma uniforme. Alarme de visão morre em até 15s (watchdog do `SafetyManager`, sem sinal do frontend). Alarme de **garra não morria nunca** — o `GripMonitor` roda no backend, ligado direto no Arduino, independente de qualquer sessão de navegador; e o próprio sinal de garra renova o watchdog a cada ~200ms de propósito (comentário already existente no código, pra não reproduzir o corte automático do firmware histórico). Resultado: sensor sem pressão + ninguém testando = buzzer/motor ligados indefinidamente, mesmo com o navegador fechado.
- Corrigido: novo `GripMonitor.PROLONGED_LOSS_S = 30.0` — depois de 30s de queda ininterrupta, o sinal de garra volta sozinho a NORMAL (sem precisar de confirmação manual). Antes disso (inclusive bem além do corte de 10s do firmware histórico), continua sem corte, como já era. Documentado como reversão PARCIAL e DELIBERADA de uma decisão de segurança anterior (celebrada como "sem corte automático" no artigo/HARDWARE.md) — escopo explicitamente "ninguém está testando agora" (dia de demonstração), não "tolerar sonolência real prolongada"; implantação real em veículo precisa reavaliar.
- **Consequência que o usuário precisa saber**: isso invalida um trecho específico do artigo reconstruído (`docs/artigo/SafeNap_CONICT_2026.docx`, ainda não commitado) — a frase "o alarme atual permanece ativo enquanto a queda persistir, sendo silenciado apenas por confirmação explícita do usuário" (Material e Métodos) e o cenário "queda mantida por 30s — sem corte automático" (Resultados, bateria de 10 cenários de garra) ficaram desatualizados. A bateria de garra agora tem 11 cenários (36→37 no total das três baterias). Artigo NÃO foi editado nesta sessão — avisado ao usuário, não corrigido silenciosamente.
- Testes: 2 novos em `test_grip_monitor.py` (1 renomeado/ajustado + 1 novo), 1 cenário renomeado + 1 novo em `test_grip_monitor_scenarios.py` (bateria de garra: 10→11, `reports/grip_scenarios/` regenerado automaticamente ao rodar os testes). pytest 139 (+2). Mutation-testado (revertido, confirmado que o cenário novo falha, restaurado). `docs/HARDWARE.md` item 4 e a docstring do módulo atualizadas.
- Próximos passos: se o usuário for submeter/apresentar o artigo reconstruído, os trechos citados acima (Material e Métodos, Resultados, e a contagem "10 cenários de garra") precisam ser atualizados de novo antes.


## [2026-09-23] Claude (Opus 5) | Auditoria de QUALIDADE da detecção (FP/FN) — 6 achados, 1 crítico de alarme falso
- Contexto: pedido do usuário para auditar profundamente a qualidade da detecção (não a arquitetura — essa já havia sido refatorada na entrada anterior), classificando cada módulo e implementando só o que fosse justificado por problema demonstrado, com regra explícita: "estou corrigindo um problema demonstrado ou apenas tornando a arquitetura mais sofisticada?".
- Baseline registrado antes de mexer: 27 arquivos, 254 testes, 1,39s.
- **Achado nº 1 (CRÍTICO, FP + FN)**: `vision/mediapipe.ts:139` — `if (analysis)` sem `else`. Quando o MediaPipe achava rosto mas `analyzeFrame` rejeitava a geometria (face pequena, landmark na borda, valor não-finito), **nem `processFrame` nem `processNoFace` eram chamados**: o quadro evaporava. Consequências: (a) `faceLostSince` nunca setado ⇒ o `PerclosTracker` não marcava o intervalo como perdido ⇒ tempo entrava no denominador como observação válida sem contribuição de fechamento (FN); (b) `markUnknown()` nunca chamado ⇒ `closedSince` e o candidato a micro-sono SOBREVIVIAM ao intervalo ⇒ olhos fechados + cabeça virando = ao voltar um quadro válido, `closedForMs` incluía o buraco inteiro ⇒ **ALARME FALSO**. Isso é provavelmente uma das causas reais dos falsos positivos relatados nos testes práticos. O docstring de `visionQuality.ts` afirmava que "os dois casos viram o mesmo caminho processNoFace() hoje" — era FALSO. Corrigido roteando para `processNoFace('DEGRADED')`.
- **Achado nº 2 (FN sistemático + dependência de FPS)**: o laço real roda a **~10 FPS** (`mediapipe.ts`: setTimeout 100ms + rAF), não 30. Com `closeConfirmFrames` (4/3/2), "3 quadros" = ~300ms no preset padrão. Piscada humana normal = 100–400ms ⇒ uma piscada de 200ms produzia 2 quadros abaixo do limiar e NUNCA confirmava ⇒ nenhum `closedSegmentEnded` ⇒ `blinkRate` ≈ 0, nada entrava no PERCLOS, feature de ML inútil. Trocado por `closeConfirmMs` (60/90/150ms) + `closeConfirmMinFrames = 2` (piso de ruído, não relógio). Mutation test PROVA o bug: com a regra antiga o teste "conta as piscadas a 10 FPS" falha (blinkRate=0) e passa a 30/60 FPS — dependência de FPS demonstrada empiricamente, não afirmada.
- **Achado nº 3 (FN + infra morta)**: `validObservedMs = windowMs - lostMs` assumia a janela de 60s inteira observada desde o 1º quadro. Aos 5s de sessão com 4s de olho fechado: `4000/60000 = 6,7%` em vez de 80%. PERCLOS praticamente inerte no primeiro minuto, reportando número baixo com aparência de medida válida. E `confidence`/`validObservedMs` eram calculados e **nunca consumidos por ninguém** (grep confirmou: só nos próprios testes). Corrigido: denominador cresce com a sessão (`min(windowMs, now - observationStart) - lostMs`) + novo `sufficient` consumido por `FusionInputs.perclosValid` — enquanto insuficiente, PERCLOS é ignorado nas 3 camadas da fusão e não bloqueia liberação de ALARM.
- **Achado nº 4 (falsa promessa arquitetural)**: `microsleepWarnMs` (900/800/600) e `earTrendAlarmFraction` (0.40/0.35/0.30) existiam nos 3 presets e **nunca eram lidos**. Escolhi REMOVER em vez de implementar (o usuário deu as duas opções): `microsleepWarnMs` seria redundante — um fechamento nesse nível já dispara PROLONGED_CLOSE via `warnCloseMs` (500–800ms); `earTrendAlarmFraction` contrariaria a política declarada de que EAR trend nunca alarma sozinho (o código já se comportava certo, a constante é que prometia o contrário).
- **Achado nº 5 (FP)**: `YawnDetector.active` só caía no ramo "boca fechada" e só após `cooldownMs` inteiro (10s) ⇒ uma abertura de 400ms gerava 10s de WARNING; e com a boca CONTINUAMENTE acima do limiar o ramo `else` nunca executava e `active` **nunca caía** (falar/rir/cantar sustentava YAWN indefinidamente). O nome `cooldownMs` prometia "não re-disparar" mas implementava "permanecer ativo". Separado em `maxMs` (7s — abertura mais longa não é bocejo), `activeHoldMs` (2s anti-flicker) e `cooldownMs` como guarda real de re-disparo.
- **Achado nº 6 (lacuna silenciosa)**: classificação de piscada devolvia `null` para a zona-morta 400–550ms. Agora `BlinkKind` explícito: artifact/normal/indeterminate/slow/prolonged. NENHUMA decisão mudou (só normal e slow alimentam taxa, como antes) — a lacuna deixou de ser invisível.
- Também: `blinkRate`/`slowBlinkRate` passaram a aceitar `validObservedMs` do PerclosTracker (reusa contabilidade testada em vez de duplicar); `FrameAnalysis.quality` (GOOD/DEGRADED por `|yawRatio| > 0.25`, o mesmo limiar que `combineEyes` já usava); `DetectionMetrics.visionQuality` publicado; `CameraView` distingue "Sem rosto" de "Ajuste a posição".
- **Espelho Python atualizado junto**: `ml/features/blink.py` (CLOSE_CONFIRM_MS, denominador de observação válida, lost intervals) e `shared/feature_golden.json` regenerado. Sem isso eu teria introduzido desvio treino↔inferência nas features `perclos`/`blinkRate`/`msSinceLastBlink` — exatamente o bug que aquele módulo existe para evitar. Paridade verificada pelos dois lados (goldenParity.test.ts + ml/tests).
- Decisões: (1) `VisionQuality` é **reportada, não usada para ponderar decisão** — rebaixar peso de sinal por yaw exigiria esquema de pesos validado, e inventar um seria apresentar chute como ciência; o que ela FAZ é manter quadros ruins fora do denominador de observação válida e diferenciar a mensagem da UI. Documentado com o experimento necessário para validar. (2) NÃO adicionei `earConfidence` numérico sem consumidor — seria recriar o problema do achado nº 4 que eu mesmo estava criticando. (3) Os 2 testes de engine que falharam (`processFrame com EAR muito baixo`, `declínio gradual`) entregavam 6–8 quadros no MESMO instante e passavam pela contagem de quadros — eles CODIFICAVAM o bug; adaptados para avançar o relógio, + teste novo dedicado provando que 6 quadros em 0ms não confirmam fechamento.
- Testes: 254 → 282 (+28). Novo `detectionEngine.robustness.test.ts` (22 testes): mesmos roteiros a 10/30/60 FPS, rosto perdido durante olhos fechados, geometria ruim com a mesma proteção, PERCLOS que não dilui com perda de rosto, bocejo que não gruda, os 3 estados de VisionQuality observáveis, PERCLOS de observação curta não gerando WARNING. pytest 89+7 skipped OK. tsc e build de produção OK. Todos os 3 fixes principais mutation-testados (revertido, confirmado que o teste correspondente falha, restaurado).
- Lições: o achado mais grave (nº 1) estava numa LINHA DE CHAMADA, não num detector — e o docstring de um módulo afirmava explicitamente que o bug não existia. Ler o caller e não confiar em comentário sobre comportamento é o que pegou. Segundo: descobrir o FPS REAL do laço (~10, não 30) reinterpretou completamente 3 parâmetros que pareciam razoáveis — auditar unidade (quadro vs. ms) sem saber a taxa real é impossível.
- Próximos passos: (1) **o modelo ONNX experimental precisa ser retreinado** — foi treinado com a semântica ANTIGA de `perclos`/`blinkRate`; como é corroborativo e já não validado isso não afeta segurança, mas invalida qualquer alegação sobre sua acurácia; (2) parâmetros novos marcados NEEDS VALIDATION com o experimento descrito (closeConfirmMs, perclosMinObservationMs, yawnMaxMs, yawnActiveHoldMs, critério de DEGRADED); (3) elevar o FPS do laço de detecção é agora a alavanca de qualidade mais direta — a 10 FPS há um teto físico de ~100ms na resolução temporal de qualquer fenômeno ocular; (4) nada disso substitui validação com dataset rotulado real (pendência de sempre).
